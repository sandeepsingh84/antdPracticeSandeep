
export const storeQuestions = ({ body }) => {
  return body;
};

export const setQuestions = ({ body }) => {
  console.log(`body`, body);
  return body;
};

export const conditions = ({ body }) => {  return body;};

export const stdTest=(payload)=>{
  return payload;
}